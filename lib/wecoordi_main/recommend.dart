import 'package:flutter/material.dart';

class recommend extends StatelessWidget {
  const recommend({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('추천'));
  }
}
